# withLazyTx（Pool / TenantTx）を呼んで良いファイル（Tx を張る入口）。scripts/check-tx.sh が突き合わせる
# DbRunner.transact / withTx: 再試行と Db.guard と RLS の印を 1 か所で決める入口
src/app/DbRunner.flix
# TenantTx: Pool.withLazyTxAfterBegin を包んで印を置く
src/app/TenantTx.flix
