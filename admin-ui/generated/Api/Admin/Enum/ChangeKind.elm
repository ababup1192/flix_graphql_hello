-- Do not manually edit this file, it was auto-generated by dillonkearns/elm-graphql
-- https://github.com/dillonkearns/elm-graphql


module Api.Admin.Enum.ChangeKind exposing (..)

import Json.Decode as Decode exposing (Decoder)


{-| フィールド（か配列の 1 要素）の変わり方

  - Added - to にだけある
  - Removed - from にだけある
  - Changed - 値が違う（OBJECT がオブジェクトでなくなった時や BLOCKS の variant が変わった時も）
  - Nested - OBJECT / BLOCKS / 配列の中で違う。children に中身
  - Text - RICH\_TEXT の Markdown の行差分。lines に中身

-}
type ChangeKind
    = Added
    | Removed
    | Changed
    | Nested
    | Text


list : List ChangeKind
list =
    [ Added, Removed, Changed, Nested, Text ]


decoder : Decoder ChangeKind
decoder =
    Decode.string
        |> Decode.andThen
            (\string ->
                case string of
                    "ADDED" ->
                        Decode.succeed Added

                    "REMOVED" ->
                        Decode.succeed Removed

                    "CHANGED" ->
                        Decode.succeed Changed

                    "NESTED" ->
                        Decode.succeed Nested

                    "TEXT" ->
                        Decode.succeed Text

                    _ ->
                        Decode.fail ("Invalid ChangeKind type, " ++ string ++ " try re-running the @dillonkearns/elm-graphql CLI ")
            )


{-| Convert from the union type representing the Enum to a string that the GraphQL server will recognize.
-}
toString : ChangeKind -> String
toString enum____ =
    case enum____ of
        Added ->
            "ADDED"

        Removed ->
            "REMOVED"

        Changed ->
            "CHANGED"

        Nested ->
            "NESTED"

        Text ->
            "TEXT"


{-| Convert from a String representation to an elm representation enum.
This is the inverse of the Enum `toString` function. So you can call `toString` and then convert back `fromString` safely.

    Swapi.Enum.Episode.NewHope
        |> Swapi.Enum.Episode.toString
        |> Swapi.Enum.Episode.fromString
        == Just NewHope

This can be useful for generating Strings to use for <select> menus to check which item was selected.

-}
fromString : String -> Maybe ChangeKind
fromString enumString____ =
    case enumString____ of
        "ADDED" ->
            Just Added

        "REMOVED" ->
            Just Removed

        "CHANGED" ->
            Just Changed

        "NESTED" ->
            Just Nested

        "TEXT" ->
            Just Text

        _ ->
            Nothing
