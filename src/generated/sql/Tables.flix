// GENERATED by sqlfx gen v6 from migrations. Do not edit.

///
/// assets テーブル。enum は断片 DSL の型に付ける印、関数は列。
///
mod AssetsTable {

    use SqlValue.SqlValue

    pub enum AssetsTable {
        case AssetsTable
    }

    pub def id(): Fragment.Col[AssetsTable, String, Fragment.NotNull] = Fragment.Col.Col("id", SqlValue.Str)
    pub def projectId(): Fragment.Col[AssetsTable, Int64, Fragment.NotNull] = Fragment.Col.Col("project_id", SqlValue.Int64)
    pub def key(): Fragment.Col[AssetsTable, String, Fragment.NotNull] = Fragment.Col.Col("key", SqlValue.Str)
    pub def fileName(): Fragment.Col[AssetsTable, String, Fragment.NotNull] = Fragment.Col.Col("file_name", SqlValue.Str)
    pub def mime(): Fragment.Col[AssetsTable, String, Fragment.NotNull] = Fragment.Col.Col("mime", SqlValue.Str)
    pub def size(): Fragment.Col[AssetsTable, Int64, Fragment.NotNull] = Fragment.Col.Col("size", SqlValue.Int64)
    pub def width(): Fragment.Col[AssetsTable, Int32, Fragment.NotNull] = Fragment.Col.Col("width", SqlValue.Int32)
    pub def height(): Fragment.Col[AssetsTable, Int32, Fragment.NotNull] = Fragment.Col.Col("height", SqlValue.Int32)
    pub def alt(): Fragment.Col[AssetsTable, String, Fragment.NotNull] = Fragment.Col.Col("alt", SqlValue.Str)
    pub def status(): Fragment.Col[AssetsTable, String, Fragment.NotNull] = Fragment.Col.Col("status", SqlValue.Str)
    pub def createdAt(): Fragment.Col[AssetsTable, Timestamp.Timestamp, Fragment.NotNull] = Fragment.Col.Col("created_at", SqlValue.ofTimestamp)

    ///
    /// DDL の名前付き制約。違反の翻訳は `onConstraint` に渡す関数で書く。case が足りなければコンパイルエラー。
    ///
    pub enum Constraint with Eq, Order, ToString {
        /// assets_key_key (unique)
        case KeyKey
        /// assets_status_check (check)
        case StatusCheck
        /// assets_project_id_fkey (foreign key)
        case ProjectIdFkey
    }

    ///
    /// このテーブルへの書き込みを、制約違反を `translate` で翻訳しながら実行する。
    /// translate は thunk と同じ型の値を返すか、業務エラーのエフェクトを投げる。翻訳しない case は `raiseConstraint`。
    /// enum に無い名前の違反と、制約違反以外の DbErr はそのまま上へ。Tx の外で呼ぶ（違反で abort した Tx に COMMIT を出さない）。
    ///
    pub def onConstraint(translate: Constraint -> a \ ef1, thunk: Unit -> a \ ef2): a \ ef1 + ef2 + {TransientDbErr, DbErr} =
        DbError.onConstraintWith(constraintOf, translate, thunk)

    ///
    /// 翻訳しない制約を元の DbErr に戻す。`onConstraint` に渡す関数の中で使う。
    ///
    pub def raiseConstraint(constraint: Constraint): a \ DbErr = match constraint {
        case Constraint.KeyKey => DbErr.uniqueViolation("assets_key_key")
        case Constraint.StatusCheck => DbErr.checkViolation("assets_status_check")
        case Constraint.ProjectIdFkey => DbErr.foreignKeyViolation("assets_project_id_fkey")
    }

    def constraintOf(name: String): Option[Constraint] = match name {
        case "assets_key_key" => Some(Constraint.KeyKey)
        case "assets_status_check" => Some(Constraint.StatusCheck)
        case "assets_project_id_fkey" => Some(Constraint.ProjectIdFkey)
        case _ => None
    }

}

///
/// content_fields テーブル。enum は断片 DSL の型に付ける印、関数は列。
///
mod ContentFieldsTable {

    use SqlValue.SqlValue

    pub enum ContentFieldsTable {
        case ContentFieldsTable
    }

    pub def id(): Fragment.Col[ContentFieldsTable, Int64, Fragment.NotNull] = Fragment.Col.Col("id", SqlValue.Int64)
    pub def typeId(): Fragment.Col[ContentFieldsTable, Int64, Fragment.NotNull] = Fragment.Col.Col("type_id", SqlValue.Int64)
    pub def apiId(): Fragment.Col[ContentFieldsTable, String, Fragment.NotNull] = Fragment.Col.Col("api_id", SqlValue.Str)
    pub def name(): Fragment.Col[ContentFieldsTable, String, Fragment.NotNull] = Fragment.Col.Col("name", SqlValue.Str)
    pub def kind(): Fragment.Col[ContentFieldsTable, String, Fragment.NotNull] = Fragment.Col.Col("kind", SqlValue.Str)
    pub def isMany(): Fragment.Col[ContentFieldsTable, Bool, Fragment.NotNull] = Fragment.Col.Col("is_many", SqlValue.Bool)
    pub def isRequired(): Fragment.Col[ContentFieldsTable, Bool, Fragment.NotNull] = Fragment.Col.Col("is_required", SqlValue.Bool)
    pub def isUnique(): Fragment.Col[ContentFieldsTable, Bool, Fragment.NotNull] = Fragment.Col.Col("is_unique", SqlValue.Bool)
    pub def isLocalized(): Fragment.Col[ContentFieldsTable, Bool, Fragment.NotNull] = Fragment.Col.Col("is_localized", SqlValue.Bool)
    pub def config(): Fragment.Col[ContentFieldsTable, Util.Json.Json, Fragment.NotNull] = Fragment.Col.Col("config", SqlValue.ofJson)
    pub def position(): Fragment.Col[ContentFieldsTable, Int32, Fragment.NotNull] = Fragment.Col.Col("position", SqlValue.Int32)
    pub def targetTypeId(): Fragment.Col[ContentFieldsTable, Int64, Fragment.Nullable] = Fragment.Col.Col("target_type_id", SqlValue.Int64)
    pub def parentFieldId(): Fragment.Col[ContentFieldsTable, Int64, Fragment.Nullable] = Fragment.Col.Col("parent_field_id", SqlValue.Int64)

    ///
    /// DDL の名前付き制約。違反の翻訳は `onConstraint` に渡す関数で書く。case が足りなければコンパイルエラー。
    ///
    pub enum Constraint with Eq, Order, ToString {
        /// content_fields_type_id_fkey (foreign key)
        case TypeIdFkey
        /// content_fields_api_id_key (unique)
        case ApiIdKey
        /// content_fields_target_type_id_fkey (foreign key)
        case TargetTypeIdFkey
        /// content_fields_parent_field_id_fkey (foreign key)
        case ParentFieldIdFkey
        /// content_fields_kind_check (check)
        case KindCheck
    }

    ///
    /// このテーブルへの書き込みを、制約違反を `translate` で翻訳しながら実行する。
    /// translate は thunk と同じ型の値を返すか、業務エラーのエフェクトを投げる。翻訳しない case は `raiseConstraint`。
    /// enum に無い名前の違反と、制約違反以外の DbErr はそのまま上へ。Tx の外で呼ぶ（違反で abort した Tx に COMMIT を出さない）。
    ///
    pub def onConstraint(translate: Constraint -> a \ ef1, thunk: Unit -> a \ ef2): a \ ef1 + ef2 + {TransientDbErr, DbErr} =
        DbError.onConstraintWith(constraintOf, translate, thunk)

    ///
    /// 翻訳しない制約を元の DbErr に戻す。`onConstraint` に渡す関数の中で使う。
    ///
    pub def raiseConstraint(constraint: Constraint): a \ DbErr = match constraint {
        case Constraint.TypeIdFkey => DbErr.foreignKeyViolation("content_fields_type_id_fkey")
        case Constraint.ApiIdKey => DbErr.uniqueViolation("content_fields_api_id_key")
        case Constraint.TargetTypeIdFkey => DbErr.foreignKeyViolation("content_fields_target_type_id_fkey")
        case Constraint.ParentFieldIdFkey => DbErr.foreignKeyViolation("content_fields_parent_field_id_fkey")
        case Constraint.KindCheck => DbErr.checkViolation("content_fields_kind_check")
    }

    def constraintOf(name: String): Option[Constraint] = match name {
        case "content_fields_type_id_fkey" => Some(Constraint.TypeIdFkey)
        case "content_fields_api_id_key" => Some(Constraint.ApiIdKey)
        case "content_fields_target_type_id_fkey" => Some(Constraint.TargetTypeIdFkey)
        case "content_fields_parent_field_id_fkey" => Some(Constraint.ParentFieldIdFkey)
        case "content_fields_kind_check" => Some(Constraint.KindCheck)
        case _ => None
    }

}

///
/// content_types テーブル。enum は断片 DSL の型に付ける印、関数は列。
///
mod ContentTypesTable {

    use SqlValue.SqlValue

    pub enum ContentTypesTable {
        case ContentTypesTable
    }

    pub def id(): Fragment.Col[ContentTypesTable, Int64, Fragment.NotNull] = Fragment.Col.Col("id", SqlValue.Int64)
    pub def apiId(): Fragment.Col[ContentTypesTable, String, Fragment.NotNull] = Fragment.Col.Col("api_id", SqlValue.Str)
    pub def kind(): Fragment.Col[ContentTypesTable, String, Fragment.NotNull] = Fragment.Col.Col("kind", SqlValue.Str)
    pub def name(): Fragment.Col[ContentTypesTable, String, Fragment.NotNull] = Fragment.Col.Col("name", SqlValue.Str)
    pub def singular(): Fragment.Col[ContentTypesTable, String, Fragment.NotNull] = Fragment.Col.Col("singular", SqlValue.Str)
    pub def plural(): Fragment.Col[ContentTypesTable, String, Fragment.NotNull] = Fragment.Col.Col("plural", SqlValue.Str)
    pub def previewUrl(): Fragment.Col[ContentTypesTable, String, Fragment.NotNull] = Fragment.Col.Col("preview_url", SqlValue.Str)
    pub def createdAt(): Fragment.Col[ContentTypesTable, Timestamp.Timestamp, Fragment.NotNull] = Fragment.Col.Col("created_at", SqlValue.ofTimestamp)
    pub def updatedAt(): Fragment.Col[ContentTypesTable, Timestamp.Timestamp, Fragment.NotNull] = Fragment.Col.Col("updated_at", SqlValue.ofTimestamp)
    pub def projectId(): Fragment.Col[ContentTypesTable, Int64, Fragment.NotNull] = Fragment.Col.Col("project_id", SqlValue.Int64)

    ///
    /// DDL の名前付き制約。違反の翻訳は `onConstraint` に渡す関数で書く。case が足りなければコンパイルエラー。
    ///
    pub enum Constraint with Eq, Order, ToString {
        /// content_types_kind_check (check)
        case KindCheck
        /// content_types_project_id_fkey (foreign key)
        case ProjectIdFkey
        /// content_types_project_api_id_key (unique)
        case ProjectApiIdKey
    }

    ///
    /// このテーブルへの書き込みを、制約違反を `translate` で翻訳しながら実行する。
    /// translate は thunk と同じ型の値を返すか、業務エラーのエフェクトを投げる。翻訳しない case は `raiseConstraint`。
    /// enum に無い名前の違反と、制約違反以外の DbErr はそのまま上へ。Tx の外で呼ぶ（違反で abort した Tx に COMMIT を出さない）。
    ///
    pub def onConstraint(translate: Constraint -> a \ ef1, thunk: Unit -> a \ ef2): a \ ef1 + ef2 + {TransientDbErr, DbErr} =
        DbError.onConstraintWith(constraintOf, translate, thunk)

    ///
    /// 翻訳しない制約を元の DbErr に戻す。`onConstraint` に渡す関数の中で使う。
    ///
    pub def raiseConstraint(constraint: Constraint): a \ DbErr = match constraint {
        case Constraint.KindCheck => DbErr.checkViolation("content_types_kind_check")
        case Constraint.ProjectIdFkey => DbErr.foreignKeyViolation("content_types_project_id_fkey")
        case Constraint.ProjectApiIdKey => DbErr.uniqueViolation("content_types_project_api_id_key")
    }

    def constraintOf(name: String): Option[Constraint] = match name {
        case "content_types_kind_check" => Some(Constraint.KindCheck)
        case "content_types_project_id_fkey" => Some(Constraint.ProjectIdFkey)
        case "content_types_project_api_id_key" => Some(Constraint.ProjectApiIdKey)
        case _ => None
    }

}

///
/// entries テーブル。enum は断片 DSL の型に付ける印、関数は列。
///
mod EntriesTable {

    use SqlValue.SqlValue

    pub enum EntriesTable {
        case EntriesTable
    }

    pub def id(): Fragment.Col[EntriesTable, String, Fragment.NotNull] = Fragment.Col.Col("id", SqlValue.Str)
    pub def typeId(): Fragment.Col[EntriesTable, Int64, Fragment.NotNull] = Fragment.Col.Col("type_id", SqlValue.Int64)
    pub def version(): Fragment.Col[EntriesTable, Int32, Fragment.NotNull] = Fragment.Col.Col("version", SqlValue.Int32)
    pub def stage(): Fragment.Col[EntriesTable, String, Fragment.NotNull] = Fragment.Col.Col("stage", SqlValue.Str)
    pub def publishedAt(): Fragment.Col[EntriesTable, Timestamp.Timestamp, Fragment.Nullable] = Fragment.Col.Col("published_at", SqlValue.ofTimestamp)
    pub def createdAt(): Fragment.Col[EntriesTable, Timestamp.Timestamp, Fragment.NotNull] = Fragment.Col.Col("created_at", SqlValue.ofTimestamp)
    pub def updatedAt(): Fragment.Col[EntriesTable, Timestamp.Timestamp, Fragment.NotNull] = Fragment.Col.Col("updated_at", SqlValue.ofTimestamp)
    pub def deletedAt(): Fragment.Col[EntriesTable, Timestamp.Timestamp, Fragment.Nullable] = Fragment.Col.Col("deleted_at", SqlValue.ofTimestamp)
    pub def projectId(): Fragment.Col[EntriesTable, Int64, Fragment.NotNull] = Fragment.Col.Col("project_id", SqlValue.Int64)

    ///
    /// DDL の名前付き制約。違反の翻訳は `onConstraint` に渡す関数で書く。case が足りなければコンパイルエラー。
    ///
    pub enum Constraint with Eq, Order, ToString {
        /// entries_type_id_fkey (foreign key)
        case TypeIdFkey
        /// entries_stage_check (check)
        case StageCheck
        /// entries_project_id_fkey (foreign key)
        case ProjectIdFkey
    }

    ///
    /// このテーブルへの書き込みを、制約違反を `translate` で翻訳しながら実行する。
    /// translate は thunk と同じ型の値を返すか、業務エラーのエフェクトを投げる。翻訳しない case は `raiseConstraint`。
    /// enum に無い名前の違反と、制約違反以外の DbErr はそのまま上へ。Tx の外で呼ぶ（違反で abort した Tx に COMMIT を出さない）。
    ///
    pub def onConstraint(translate: Constraint -> a \ ef1, thunk: Unit -> a \ ef2): a \ ef1 + ef2 + {TransientDbErr, DbErr} =
        DbError.onConstraintWith(constraintOf, translate, thunk)

    ///
    /// 翻訳しない制約を元の DbErr に戻す。`onConstraint` に渡す関数の中で使う。
    ///
    pub def raiseConstraint(constraint: Constraint): a \ DbErr = match constraint {
        case Constraint.TypeIdFkey => DbErr.foreignKeyViolation("entries_type_id_fkey")
        case Constraint.StageCheck => DbErr.checkViolation("entries_stage_check")
        case Constraint.ProjectIdFkey => DbErr.foreignKeyViolation("entries_project_id_fkey")
    }

    def constraintOf(name: String): Option[Constraint] = match name {
        case "entries_type_id_fkey" => Some(Constraint.TypeIdFkey)
        case "entries_stage_check" => Some(Constraint.StageCheck)
        case "entries_project_id_fkey" => Some(Constraint.ProjectIdFkey)
        case _ => None
    }

}

///
/// entry_contents テーブル。enum は断片 DSL の型に付ける印、関数は列。
///
mod EntryContentsTable {

    use SqlValue.SqlValue

    pub enum EntryContentsTable {
        case EntryContentsTable
    }

    pub def entryId(): Fragment.Col[EntryContentsTable, String, Fragment.NotNull] = Fragment.Col.Col("entry_id", SqlValue.Str)
    pub def stage(): Fragment.Col[EntryContentsTable, String, Fragment.NotNull] = Fragment.Col.Col("stage", SqlValue.Str)
    pub def data(): Fragment.Col[EntryContentsTable, Util.Json.Json, Fragment.NotNull] = Fragment.Col.Col("data", SqlValue.ofJson)
    pub def updatedAt(): Fragment.Col[EntryContentsTable, Timestamp.Timestamp, Fragment.NotNull] = Fragment.Col.Col("updated_at", SqlValue.ofTimestamp)

    ///
    /// DDL の名前付き制約。違反の翻訳は `onConstraint` に渡す関数で書く。case が足りなければコンパイルエラー。
    ///
    pub enum Constraint with Eq, Order, ToString {
        /// entry_contents_entry_id_fkey (foreign key)
        case EntryIdFkey
        /// entry_contents_stage_check (check)
        case StageCheck
    }

    ///
    /// このテーブルへの書き込みを、制約違反を `translate` で翻訳しながら実行する。
    /// translate は thunk と同じ型の値を返すか、業務エラーのエフェクトを投げる。翻訳しない case は `raiseConstraint`。
    /// enum に無い名前の違反と、制約違反以外の DbErr はそのまま上へ。Tx の外で呼ぶ（違反で abort した Tx に COMMIT を出さない）。
    ///
    pub def onConstraint(translate: Constraint -> a \ ef1, thunk: Unit -> a \ ef2): a \ ef1 + ef2 + {TransientDbErr, DbErr} =
        DbError.onConstraintWith(constraintOf, translate, thunk)

    ///
    /// 翻訳しない制約を元の DbErr に戻す。`onConstraint` に渡す関数の中で使う。
    ///
    pub def raiseConstraint(constraint: Constraint): a \ DbErr = match constraint {
        case Constraint.EntryIdFkey => DbErr.foreignKeyViolation("entry_contents_entry_id_fkey")
        case Constraint.StageCheck => DbErr.checkViolation("entry_contents_stage_check")
    }

    def constraintOf(name: String): Option[Constraint] = match name {
        case "entry_contents_entry_id_fkey" => Some(Constraint.EntryIdFkey)
        case "entry_contents_stage_check" => Some(Constraint.StageCheck)
        case _ => None
    }

}

///
/// entry_links テーブル。enum は断片 DSL の型に付ける印、関数は列。
///
mod EntryLinksTable {

    use SqlValue.SqlValue

    pub enum EntryLinksTable {
        case EntryLinksTable
    }

    pub def fromEntryId(): Fragment.Col[EntryLinksTable, String, Fragment.NotNull] = Fragment.Col.Col("from_entry_id", SqlValue.Str)
    pub def stage(): Fragment.Col[EntryLinksTable, String, Fragment.NotNull] = Fragment.Col.Col("stage", SqlValue.Str)
    pub def fieldId(): Fragment.Col[EntryLinksTable, Int64, Fragment.NotNull] = Fragment.Col.Col("field_id", SqlValue.Int64)
    pub def toEntryId(): Fragment.Col[EntryLinksTable, String, Fragment.NotNull] = Fragment.Col.Col("to_entry_id", SqlValue.Str)
    pub def position(): Fragment.Col[EntryLinksTable, Int32, Fragment.NotNull] = Fragment.Col.Col("position", SqlValue.Int32)

    ///
    /// DDL の名前付き制約。違反の翻訳は `onConstraint` に渡す関数で書く。case が足りなければコンパイルエラー。
    ///
    pub enum Constraint with Eq, Order, ToString {
        /// entry_links_from_fkey (foreign key)
        case FromFkey
        /// entry_links_field_fkey (foreign key)
        case FieldFkey
        /// entry_links_stage_check (check)
        case StageCheck
    }

    ///
    /// このテーブルへの書き込みを、制約違反を `translate` で翻訳しながら実行する。
    /// translate は thunk と同じ型の値を返すか、業務エラーのエフェクトを投げる。翻訳しない case は `raiseConstraint`。
    /// enum に無い名前の違反と、制約違反以外の DbErr はそのまま上へ。Tx の外で呼ぶ（違反で abort した Tx に COMMIT を出さない）。
    ///
    pub def onConstraint(translate: Constraint -> a \ ef1, thunk: Unit -> a \ ef2): a \ ef1 + ef2 + {TransientDbErr, DbErr} =
        DbError.onConstraintWith(constraintOf, translate, thunk)

    ///
    /// 翻訳しない制約を元の DbErr に戻す。`onConstraint` に渡す関数の中で使う。
    ///
    pub def raiseConstraint(constraint: Constraint): a \ DbErr = match constraint {
        case Constraint.FromFkey => DbErr.foreignKeyViolation("entry_links_from_fkey")
        case Constraint.FieldFkey => DbErr.foreignKeyViolation("entry_links_field_fkey")
        case Constraint.StageCheck => DbErr.checkViolation("entry_links_stage_check")
    }

    def constraintOf(name: String): Option[Constraint] = match name {
        case "entry_links_from_fkey" => Some(Constraint.FromFkey)
        case "entry_links_field_fkey" => Some(Constraint.FieldFkey)
        case "entry_links_stage_check" => Some(Constraint.StageCheck)
        case _ => None
    }

}

///
/// entry_versions テーブル。enum は断片 DSL の型に付ける印、関数は列。
///
mod EntryVersionsTable {

    use SqlValue.SqlValue

    pub enum EntryVersionsTable {
        case EntryVersionsTable
    }

    pub def id(): Fragment.Col[EntryVersionsTable, Int64, Fragment.NotNull] = Fragment.Col.Col("id", SqlValue.Int64)
    pub def entryId(): Fragment.Col[EntryVersionsTable, String, Fragment.NotNull] = Fragment.Col.Col("entry_id", SqlValue.Str)
    pub def version(): Fragment.Col[EntryVersionsTable, Int32, Fragment.NotNull] = Fragment.Col.Col("version", SqlValue.Int32)
    pub def data(): Fragment.Col[EntryVersionsTable, Util.Json.Json, Fragment.NotNull] = Fragment.Col.Col("data", SqlValue.ofJson)
    pub def author(): Fragment.Col[EntryVersionsTable, String, Fragment.NotNull] = Fragment.Col.Col("author", SqlValue.Str)
    pub def reason(): Fragment.Col[EntryVersionsTable, String, Fragment.NotNull] = Fragment.Col.Col("reason", SqlValue.Str)
    pub def createdAt(): Fragment.Col[EntryVersionsTable, Timestamp.Timestamp, Fragment.NotNull] = Fragment.Col.Col("created_at", SqlValue.ofTimestamp)

    ///
    /// DDL の名前付き制約。違反の翻訳は `onConstraint` に渡す関数で書く。case が足りなければコンパイルエラー。
    ///
    pub enum Constraint with Eq, Order, ToString {
        /// entry_versions_entry_id_fkey (foreign key)
        case EntryIdFkey
        /// entry_versions_reason_check (check)
        case ReasonCheck
    }

    ///
    /// このテーブルへの書き込みを、制約違反を `translate` で翻訳しながら実行する。
    /// translate は thunk と同じ型の値を返すか、業務エラーのエフェクトを投げる。翻訳しない case は `raiseConstraint`。
    /// enum に無い名前の違反と、制約違反以外の DbErr はそのまま上へ。Tx の外で呼ぶ（違反で abort した Tx に COMMIT を出さない）。
    ///
    pub def onConstraint(translate: Constraint -> a \ ef1, thunk: Unit -> a \ ef2): a \ ef1 + ef2 + {TransientDbErr, DbErr} =
        DbError.onConstraintWith(constraintOf, translate, thunk)

    ///
    /// 翻訳しない制約を元の DbErr に戻す。`onConstraint` に渡す関数の中で使う。
    ///
    pub def raiseConstraint(constraint: Constraint): a \ DbErr = match constraint {
        case Constraint.EntryIdFkey => DbErr.foreignKeyViolation("entry_versions_entry_id_fkey")
        case Constraint.ReasonCheck => DbErr.checkViolation("entry_versions_reason_check")
    }

    def constraintOf(name: String): Option[Constraint] = match name {
        case "entry_versions_entry_id_fkey" => Some(Constraint.EntryIdFkey)
        case "entry_versions_reason_check" => Some(Constraint.ReasonCheck)
        case _ => None
    }

}

///
/// projects テーブル。enum は断片 DSL の型に付ける印、関数は列。
///
mod ProjectsTable {

    use SqlValue.SqlValue

    pub enum ProjectsTable {
        case ProjectsTable
    }

    pub def id(): Fragment.Col[ProjectsTable, Int64, Fragment.NotNull] = Fragment.Col.Col("id", SqlValue.Int64)
    pub def slug(): Fragment.Col[ProjectsTable, String, Fragment.NotNull] = Fragment.Col.Col("slug", SqlValue.Str)
    pub def name(): Fragment.Col[ProjectsTable, String, Fragment.NotNull] = Fragment.Col.Col("name", SqlValue.Str)
    pub def createdAt(): Fragment.Col[ProjectsTable, Timestamp.Timestamp, Fragment.NotNull] = Fragment.Col.Col("created_at", SqlValue.ofTimestamp)

    ///
    /// DDL の名前付き制約。違反の翻訳は `onConstraint` に渡す関数で書く。case が足りなければコンパイルエラー。
    ///
    pub enum Constraint with Eq, Order, ToString {
        /// projects_slug_key (unique)
        case SlugKey
    }

    ///
    /// このテーブルへの書き込みを、制約違反を `translate` で翻訳しながら実行する。
    /// translate は thunk と同じ型の値を返すか、業務エラーのエフェクトを投げる。翻訳しない case は `raiseConstraint`。
    /// enum に無い名前の違反と、制約違反以外の DbErr はそのまま上へ。Tx の外で呼ぶ（違反で abort した Tx に COMMIT を出さない）。
    ///
    pub def onConstraint(translate: Constraint -> a \ ef1, thunk: Unit -> a \ ef2): a \ ef1 + ef2 + {TransientDbErr, DbErr} =
        DbError.onConstraintWith(constraintOf, translate, thunk)

    ///
    /// 翻訳しない制約を元の DbErr に戻す。`onConstraint` に渡す関数の中で使う。
    ///
    pub def raiseConstraint(constraint: Constraint): a \ DbErr = match constraint {
        case Constraint.SlugKey => DbErr.uniqueViolation("projects_slug_key")
    }

    def constraintOf(name: String): Option[Constraint] = match name {
        case "projects_slug_key" => Some(Constraint.SlugKey)
        case _ => None
    }

}
