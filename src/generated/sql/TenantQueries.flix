// GENERATED by sqlfx gen v7 from tenant.q. Do not edit.
mod TenantQueries {

    use SqlValue.SqlValue
    use Decoder.Decoder

    ///
    /// 生成元 tenant.q のハッシュ。テストで .q の現物と照合する。
    ///
    pub def sourceHash(): Int32 = 1960673228

    ///
    /// stampProject の 1 行。
    ///
    pub type alias StampProjectRow = { applied = Option[String] }

    def stampProjectDecoder(): Decoder[StampProjectRow] =
        forA (
            applied <- Decoder.opt(Decoder.str("applied"))
        ) yield { applied = applied }

    ///
    /// stampProject: SELECT set_config('app.project_id', $1, true)::text AS applied
    ///
    pub def stampProject(projectId: String): Option[StampProjectRow] \ DbRead =
        let sql = "SELECT set_config('app.project_id', $1, true)::text AS applied";
        let params = List#{SqlValue.Str(projectId)};
        RawSql.runWithAllow(() -> Sql.fetchOneAs(stampProjectDecoder(), sql, params))

    ///
    /// stampProjectSession の 1 行。
    ///
    pub type alias StampProjectSessionRow = { applied = Option[String] }

    def stampProjectSessionDecoder(): Decoder[StampProjectSessionRow] =
        forA (
            applied <- Decoder.opt(Decoder.str("applied"))
        ) yield { applied = applied }

    ///
    /// stampProjectSession: SELECT set_config('app.project_id', $1, false)::text AS applied
    ///
    pub def stampProjectSession(projectId: String): Option[StampProjectSessionRow] \ DbRead =
        let sql = "SELECT set_config('app.project_id', $1, false)::text AS applied";
        let params = List#{SqlValue.Str(projectId)};
        RawSql.runWithAllow(() -> Sql.fetchOneAs(stampProjectSessionDecoder(), sql, params))

    ///
    /// stampTokenHash の 1 行。
    ///
    pub type alias StampTokenHashRow = { applied = Option[String] }

    def stampTokenHashDecoder(): Decoder[StampTokenHashRow] =
        forA (
            applied <- Decoder.opt(Decoder.str("applied"))
        ) yield { applied = applied }

    ///
    /// stampTokenHash: SELECT set_config('app.token_hash', $1, true)::text AS applied
    ///
    pub def stampTokenHash(tokenHash: String): Option[StampTokenHashRow] \ DbRead =
        let sql = "SELECT set_config('app.token_hash', $1, true)::text AS applied";
        let params = List#{SqlValue.Str(tokenHash)};
        RawSql.runWithAllow(() -> Sql.fetchOneAs(stampTokenHashDecoder(), sql, params))

    ///
    /// stampUser の 1 行。
    ///
    pub type alias StampUserRow = { applied = Option[String], emailApplied = Option[String] }

    def stampUserDecoder(): Decoder[StampUserRow] =
        forA (
            applied <- Decoder.opt(Decoder.str("applied"));
            emailApplied <- Decoder.opt(Decoder.str("email_applied"))
        ) yield { applied = applied, emailApplied = emailApplied }

    ///
    /// stampUser: SELECT set_config('app.user_id', $1, true)::text AS applied, set_config('app.email', $2, true)::text AS email_applied
    ///
    pub def stampUser(args: { userId = String, email = String }): Option[StampUserRow] \ DbRead =
        let sql = "SELECT set_config('app.user_id', $1, true)::text AS applied, set_config('app.email', $2, true)::text AS email_applied";
        let params = List#{SqlValue.Str(args#userId), SqlValue.Str(args#email)};
        RawSql.runWithAllow(() -> Sql.fetchOneAs(stampUserDecoder(), sql, params))

}
