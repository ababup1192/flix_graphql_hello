// GENERATED by sqlfx gen v6 from entries.q. Do not edit.
mod EntriesQueries {

    use SqlValue.SqlValue
    use Decoder.Decoder

    ///
    /// 生成元 entries.q のハッシュ。テストで .q の現物と照合する。
    ///
    pub def sourceHash(): Int32 = -1549181074

    ///
    /// findEntry の 1 行。
    ///
    pub type alias FindEntryRow = { id = String, typeId = Int64, version = Int32, stage = String, publishedAt = Option[Timestamp.Timestamp], createdAt = Timestamp.Timestamp, updatedAt = Timestamp.Timestamp, data = Util.Json.Json }

    def findEntryDecoder(): Decoder[FindEntryRow] =
        forA (
            id <- Decoder.str("id");
            typeId <- Decoder.int64("type_id");
            version <- Decoder.int32("version");
            stage <- Decoder.str("stage");
            publishedAt <- Decoder.opt(Decoder.timestamp("published_at"));
            createdAt <- Decoder.timestamp("created_at");
            updatedAt <- Decoder.timestamp("updated_at");
            data <- Decoder.json("data")
        ) yield { id = id, typeId = typeId, version = version, stage = stage, publishedAt = publishedAt, createdAt = createdAt, updatedAt = updatedAt, data = data }

    ///
    /// findEntry: SELECT e.id, e.type_id, e.version, e.stage, e.published_at, e.created_at, e.updated_at, c.data FROM entries AS e JOIN entry_contents AS c ON c.entry_id = e.id AND c.stage = 'draft' WHERE e.id = $1 AND e.deleted_at IS NULL
    ///
    pub def findEntry(id: String): Option[FindEntryRow] \ DbRead =
        let sql = "SELECT e.id, e.type_id, e.version, e.stage, e.published_at, e.created_at, e.updated_at, c.data\n    FROM entries AS e\n    JOIN entry_contents AS c ON c.entry_id = e.id AND c.stage = 'draft'\n    WHERE e.id = $1 AND e.deleted_at IS NULL";
        let params = List#{SqlValue.Str(id)};
        RawSql.runWithAllow(() -> Sql.fetchOneAs(findEntryDecoder(), sql, params))

    ///
    /// listEntries の 1 行。
    ///
    pub type alias ListEntriesRow = { id = String, typeId = Int64, version = Int32, stage = String, publishedAt = Option[Timestamp.Timestamp], createdAt = Timestamp.Timestamp, updatedAt = Timestamp.Timestamp, data = Util.Json.Json }

    def listEntriesDecoder(): Decoder[ListEntriesRow] =
        forA (
            id <- Decoder.str("id");
            typeId <- Decoder.int64("type_id");
            version <- Decoder.int32("version");
            stage <- Decoder.str("stage");
            publishedAt <- Decoder.opt(Decoder.timestamp("published_at"));
            createdAt <- Decoder.timestamp("created_at");
            updatedAt <- Decoder.timestamp("updated_at");
            data <- Decoder.json("data")
        ) yield { id = id, typeId = typeId, version = version, stage = stage, publishedAt = publishedAt, createdAt = createdAt, updatedAt = updatedAt, data = data }

    ///
    /// listEntries: SELECT e.id, e.type_id, e.version, e.stage, e.published_at, e.created_at, e.updated_at, c.data FROM entries AS e JOIN entry_contents AS c ON c.entry_id = e.id AND c.stage = 'draft' WHERE e.type_id = $1 AND e.deleted_at IS NULL AND ($2 = '' OR EXISTS (SELECT 1 FROM jsonb_each_text(c.data) AS kv WHERE kv.value ILIKE '%' || $2 || '%')) ORDER BY e.updated_at DESC, e.id LIMIT $3 OFFSET $4
    ///
    pub def listEntries(args: { typeId = Int64, search = String, limit = Int64, offset = Int64 }): List[ListEntriesRow] \ DbRead =
        let sql = "SELECT e.id, e.type_id, e.version, e.stage, e.published_at, e.created_at, e.updated_at, c.data\n    FROM entries AS e\n    JOIN entry_contents AS c ON c.entry_id = e.id AND c.stage = 'draft'\n    WHERE e.type_id = $1 AND e.deleted_at IS NULL\n      AND ($2 = '' OR EXISTS (SELECT 1 FROM jsonb_each_text(c.data) AS kv WHERE kv.value ILIKE '%' || $2 || '%'))\n    ORDER BY e.updated_at DESC, e.id\n    LIMIT $3 OFFSET $4";
        let params = List#{SqlValue.Int64(args#typeId), SqlValue.Str(args#search), SqlValue.Int64(args#limit), SqlValue.Int64(args#offset)};
        RawSql.runWithAllow(() -> Sql.fetchAs(listEntriesDecoder(), sql, params))

    ///
    /// countEntries の 1 行。
    ///
    pub type alias CountEntriesRow = { total = Option[Int64] }

    def countEntriesDecoder(): Decoder[CountEntriesRow] =
        forA (
            total <- Decoder.opt(Decoder.int64("total"))
        ) yield { total = total }

    ///
    /// countEntries: SELECT count(*)::bigint AS total FROM entries AS e JOIN entry_contents AS c ON c.entry_id = e.id AND c.stage = 'draft' WHERE e.type_id = $1 AND e.deleted_at IS NULL AND ($2 = '' OR EXISTS (SELECT 1 FROM jsonb_each_text(c.data) AS kv WHERE kv.value ILIKE '%' || $2 || '%'))
    ///
    pub def countEntries(args: { typeId = Int64, search = String }): Option[CountEntriesRow] \ DbRead =
        let sql = "SELECT count(*)::bigint AS total\n    FROM entries AS e\n    JOIN entry_contents AS c ON c.entry_id = e.id AND c.stage = 'draft'\n    WHERE e.type_id = $1 AND e.deleted_at IS NULL\n      AND ($2 = '' OR EXISTS (SELECT 1 FROM jsonb_each_text(c.data) AS kv WHERE kv.value ILIKE '%' || $2 || '%'))";
        let params = List#{SqlValue.Int64(args#typeId), SqlValue.Str(args#search)};
        RawSql.runWithAllow(() -> Sql.fetchOneAs(countEntriesDecoder(), sql, params))

    ///
    /// entryExists の 1 行。
    ///
    pub type alias EntryExistsRow = { id = String }

    def entryExistsDecoder(): Decoder[EntryExistsRow] =
        forA (
            id <- Decoder.str("id")
        ) yield { id = id }

    ///
    /// entryExists: SELECT id FROM entries WHERE id = $1
    ///
    pub def entryExists(id: String): Option[EntryExistsRow] \ DbRead =
        let sql = "SELECT id FROM entries WHERE id = $1";
        let params = List#{SqlValue.Str(id)};
        RawSql.runWithAllow(() -> Sql.fetchOneAs(entryExistsDecoder(), sql, params))

    ///
    /// countEntriesOfType の 1 行。
    ///
    pub type alias CountEntriesOfTypeRow = { total = Option[Int64] }

    def countEntriesOfTypeDecoder(): Decoder[CountEntriesOfTypeRow] =
        forA (
            total <- Decoder.opt(Decoder.int64("total"))
        ) yield { total = total }

    ///
    /// countEntriesOfType: SELECT count(*)::bigint AS total FROM entries WHERE type_id = $1 AND deleted_at IS NULL
    ///
    pub def countEntriesOfType(typeId: Int64): Option[CountEntriesOfTypeRow] \ DbRead =
        let sql = "SELECT count(*)::bigint AS total FROM entries WHERE type_id = $1 AND deleted_at IS NULL";
        let params = List#{SqlValue.Int64(typeId)};
        RawSql.runWithAllow(() -> Sql.fetchOneAs(countEntriesOfTypeDecoder(), sql, params))

    ///
    /// insertEntry: INSERT INTO entries (id, type_id) VALUES ($1, $2)
    ///
    pub def insertEntry(args: { id = String, typeId = Int64 }): Int32 \ DbWrite =
        let sql = "INSERT INTO entries (id, type_id) VALUES ($1, $2)";
        let params = List#{SqlValue.Str(args#id), SqlValue.Int64(args#typeId)};
        RawSql.runWithAllow(() -> Sql.execute(sql, params))

    ///
    /// bumpEntryVersion: UPDATE entries SET version = version + 1, updated_at = now() WHERE id = $1 AND version = $2 AND deleted_at IS NULL
    ///
    pub def bumpEntryVersion(args: { id = String, expected = Int32 }): Int32 \ DbWrite =
        let sql = "UPDATE entries SET version = version + 1, updated_at = now()\n    WHERE id = $1 AND version = $2 AND deleted_at IS NULL";
        let params = List#{SqlValue.Str(args#id), SqlValue.Int32(args#expected)};
        RawSql.runWithAllow(() -> Sql.execute(sql, params))

    ///
    /// softDeleteEntry: UPDATE entries SET deleted_at = now() WHERE id = $1 AND deleted_at IS NULL
    ///
    pub def softDeleteEntry(id: String): Int32 \ DbWrite =
        let sql = "UPDATE entries SET deleted_at = now() WHERE id = $1 AND deleted_at IS NULL";
        let params = List#{SqlValue.Str(id)};
        RawSql.runWithAllow(() -> Sql.execute(sql, params))

    ///
    /// upsertContent: INSERT INTO entry_contents (entry_id, stage, data) VALUES ($1, $2, $3) ON CONFLICT (entry_id, stage) DO UPDATE SET data = EXCLUDED.data, updated_at = now()
    ///
    pub def upsertContent(args: { entryId = String, stage = String, data = Util.Json.Json }): Int32 \ DbWrite =
        let sql = "INSERT INTO entry_contents (entry_id, stage, data) VALUES ($1, $2, $3)\n    ON CONFLICT (entry_id, stage) DO UPDATE SET data = EXCLUDED.data, updated_at = now()";
        let params = List#{SqlValue.Str(args#entryId), SqlValue.Str(args#stage), SqlValue.ofJson(args#data)};
        RawSql.runWithAllow(() -> Sql.execute(sql, params))

}
