// GENERATED by sqlfx gen v7 from entry_autosaves.q. Do not edit.
mod EntryAutosavesQueries {

    use SqlValue.SqlValue
    use Decoder.Decoder

    ///
    /// 生成元 entry_autosaves.q のハッシュ。テストで .q の現物と照合する。
    ///
    pub def sourceHash(): Int32 = 862470480

    ///
    /// upsertAutosave: INSERT INTO entry_autosaves (entry_id, user_id, project_id, data, base_version) VALUES ($1, $2, $3, $4, $5) ON CONFLICT (entry_id, user_id) DO UPDATE SET data = EXCLUDED.data, base_version = EXCLUDED.base_version, updated_at = now()
    ///
    pub def upsertAutosave(args: { entryId = String, userId = Int64, projectId = Int64, data = Util.Json.Json, baseVersion = Int32 }): Int32 \ DbWrite =
        let sql = "INSERT INTO entry_autosaves (entry_id, user_id, project_id, data, base_version)\n    VALUES ($1, $2, $3, $4, $5)\n    ON CONFLICT (entry_id, user_id) DO UPDATE SET data = EXCLUDED.data, base_version = EXCLUDED.base_version, updated_at = now()";
        let params = List#{SqlValue.Str(args#entryId), SqlValue.Int64(args#userId), SqlValue.Int64(args#projectId), SqlValue.ofJson(args#data), SqlValue.Int32(args#baseVersion)};
        RawSql.runWithAllow(() -> Sql.execute(sql, params))

    ///
    /// findAutosave の 1 行。
    ///
    pub type alias FindAutosaveRow = { entryId = String, userId = Int64, data = Util.Json.Json, baseVersion = Int32, updatedAt = Timestamp.Timestamp }

    def findAutosaveDecoder(): Decoder[FindAutosaveRow] =
        forA (
            entryId <- Decoder.str("entry_id");
            userId <- Decoder.int64("user_id");
            data <- Decoder.json("data");
            baseVersion <- Decoder.int32("base_version");
            updatedAt <- Decoder.timestamp("updated_at")
        ) yield { entryId = entryId, userId = userId, data = data, baseVersion = baseVersion, updatedAt = updatedAt }

    ///
    /// findAutosave: SELECT entry_id, user_id, data, base_version, updated_at FROM entry_autosaves WHERE entry_id = $1 AND user_id = $2 AND project_id = $3
    ///
    pub def findAutosave(args: { entryId = String, userId = Int64, projectId = Int64 }): Option[FindAutosaveRow] \ DbRead =
        let sql = "SELECT entry_id, user_id, data, base_version, updated_at\n    FROM entry_autosaves\n    WHERE entry_id = $1 AND user_id = $2 AND project_id = $3";
        let params = List#{SqlValue.Str(args#entryId), SqlValue.Int64(args#userId), SqlValue.Int64(args#projectId)};
        RawSql.runWithAllow(() -> Sql.fetchOneAs(findAutosaveDecoder(), sql, params))

    ///
    /// findAutosavesOf の 1 行。
    ///
    pub type alias FindAutosavesOfRow = { entryId = String, userId = Int64, data = Util.Json.Json, baseVersion = Int32, updatedAt = Timestamp.Timestamp }

    def findAutosavesOfDecoder(): Decoder[FindAutosavesOfRow] =
        forA (
            entryId <- Decoder.str("entry_id");
            userId <- Decoder.int64("user_id");
            data <- Decoder.json("data");
            baseVersion <- Decoder.int32("base_version");
            updatedAt <- Decoder.timestamp("updated_at")
        ) yield { entryId = entryId, userId = userId, data = data, baseVersion = baseVersion, updatedAt = updatedAt }

    ///
    /// findAutosavesOf: SELECT entry_id, user_id, data, base_version, updated_at FROM entry_autosaves WHERE entry_id = ANY($1) AND user_id = $2 AND project_id = $3
    ///
    pub def findAutosavesOf(args: { entryIds = List[String], userId = Int64, projectId = Int64 }): List[FindAutosavesOfRow] \ DbRead =
        let sql = "SELECT entry_id, user_id, data, base_version, updated_at\n    FROM entry_autosaves\n    WHERE entry_id = ANY($1) AND user_id = $2 AND project_id = $3";
        let params = List#{SqlValue.StrArray(args#entryIds), SqlValue.Int64(args#userId), SqlValue.Int64(args#projectId)};
        RawSql.runWithAllow(() -> Sql.fetchAs(findAutosavesOfDecoder(), sql, params))

    ///
    /// deleteAutosave: DELETE FROM entry_autosaves WHERE entry_id = $1 AND user_id = $2 AND project_id = $3
    ///
    pub def deleteAutosave(args: { entryId = String, userId = Int64, projectId = Int64 }): Int32 \ DbWrite =
        let sql = "DELETE FROM entry_autosaves WHERE entry_id = $1 AND user_id = $2 AND project_id = $3";
        let params = List#{SqlValue.Str(args#entryId), SqlValue.Int64(args#userId), SqlValue.Int64(args#projectId)};
        RawSql.runWithAllow(() -> Sql.execute(sql, params))

    ///
    /// deleteAutosavesOfEntry: DELETE FROM entry_autosaves WHERE entry_id = $1 AND project_id = $2
    ///
    pub def deleteAutosavesOfEntry(args: { entryId = String, projectId = Int64 }): Int32 \ DbWrite =
        let sql = "DELETE FROM entry_autosaves WHERE entry_id = $1 AND project_id = $2";
        let params = List#{SqlValue.Str(args#entryId), SqlValue.Int64(args#projectId)};
        RawSql.runWithAllow(() -> Sql.execute(sql, params))

}
