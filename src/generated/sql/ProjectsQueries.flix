// GENERATED by sqlfx gen v7 from projects.q. Do not edit.
mod ProjectsQueries {

    use SqlValue.SqlValue
    use Decoder.Decoder

    ///
    /// 生成元 projects.q のハッシュ。テストで .q の現物と照合する。
    ///
    pub def sourceHash(): Int32 = -891895404

    ///
    /// findProjectBySlug の 1 行。
    ///
    pub type alias FindProjectBySlugRow = { id = Int64, publicId = String, slug = String, name = String, orgId = Int64, visibility = String }

    def findProjectBySlugDecoder(): Decoder[FindProjectBySlugRow] =
        forA (
            id <- Decoder.int64("id");
            publicId <- Decoder.str("public_id");
            slug <- Decoder.str("slug");
            name <- Decoder.str("name");
            orgId <- Decoder.int64("org_id");
            visibility <- Decoder.str("visibility")
        ) yield { id = id, publicId = publicId, slug = slug, name = name, orgId = orgId, visibility = visibility }

    ///
    /// findProjectBySlug: SELECT id, public_id, slug, name, org_id, visibility FROM projects WHERE slug = $1
    ///
    pub def findProjectBySlug(slug: String): Option[FindProjectBySlugRow] \ DbRead =
        let sql = "SELECT id, public_id, slug, name, org_id, visibility FROM projects WHERE slug = $1";
        let params = List#{SqlValue.Str(slug)};
        RawSql.runWithAllow(() -> Sql.fetchOneAs(findProjectBySlugDecoder(), sql, params))

    ///
    /// findProjectById の 1 行。
    ///
    pub type alias FindProjectByIdRow = { id = Int64, publicId = String, slug = String, name = String, orgId = Int64, visibility = String }

    def findProjectByIdDecoder(): Decoder[FindProjectByIdRow] =
        forA (
            id <- Decoder.int64("id");
            publicId <- Decoder.str("public_id");
            slug <- Decoder.str("slug");
            name <- Decoder.str("name");
            orgId <- Decoder.int64("org_id");
            visibility <- Decoder.str("visibility")
        ) yield { id = id, publicId = publicId, slug = slug, name = name, orgId = orgId, visibility = visibility }

    ///
    /// findProjectById: SELECT id, public_id, slug, name, org_id, visibility FROM projects WHERE id = $1
    ///
    pub def findProjectById(id: Int64): Option[FindProjectByIdRow] \ DbRead =
        let sql = "SELECT id, public_id, slug, name, org_id, visibility FROM projects WHERE id = $1";
        let params = List#{SqlValue.Int64(id)};
        RawSql.runWithAllow(() -> Sql.fetchOneAs(findProjectByIdDecoder(), sql, params))

    ///
    /// listProjects の 1 行。
    ///
    pub type alias ListProjectsRow = { id = Int64, publicId = String, slug = String, name = String, orgId = Int64, visibility = String }

    def listProjectsDecoder(): Decoder[ListProjectsRow] =
        forA (
            id <- Decoder.int64("id");
            publicId <- Decoder.str("public_id");
            slug <- Decoder.str("slug");
            name <- Decoder.str("name");
            orgId <- Decoder.int64("org_id");
            visibility <- Decoder.str("visibility")
        ) yield { id = id, publicId = publicId, slug = slug, name = name, orgId = orgId, visibility = visibility }

    ///
    /// listProjects: SELECT id, public_id, slug, name, org_id, visibility FROM projects ORDER BY id
    ///
    pub def listProjects(): List[ListProjectsRow] \ DbRead =
        let sql = "SELECT id, public_id, slug, name, org_id, visibility FROM projects ORDER BY id";
        let params = Nil;
        RawSql.runWithAllow(() -> Sql.fetchAs(listProjectsDecoder(), sql, params))

    ///
    /// insertProject の 1 行。
    ///
    pub type alias InsertProjectRow = { id = Int64 }

    def insertProjectDecoder(): Decoder[InsertProjectRow] =
        forA (
            id <- Decoder.int64("id")
        ) yield { id = id }

    ///
    /// insertProject: INSERT INTO projects (public_id, org_id, slug, name) VALUES ($1, $2, $3, $4) RETURNING id
    ///
    pub def insertProject(args: { publicId = String, orgId = Int64, slug = String, name = String }): Option[InsertProjectRow] \ DbWrite =
        let sql = "INSERT INTO projects (public_id, org_id, slug, name) VALUES ($1, $2, $3, $4) RETURNING id";
        let params = List#{SqlValue.Str(args#publicId), SqlValue.Int64(args#orgId), SqlValue.Str(args#slug), SqlValue.Str(args#name)};
        RawSql.runWithAllow(() -> Sql.executeReturningOneAs(insertProjectDecoder(), sql, params))

    ///
    /// findContentVersion の 1 行。
    ///
    pub type alias FindContentVersionRow = { contentVersion = Int64 }

    def findContentVersionDecoder(): Decoder[FindContentVersionRow] =
        forA (
            contentVersion <- Decoder.int64("content_version")
        ) yield { contentVersion = contentVersion }

    ///
    /// findContentVersion: SELECT content_version FROM projects WHERE id = $1
    ///
    pub def findContentVersion(id: Int64): Option[FindContentVersionRow] \ DbRead =
        let sql = "SELECT content_version FROM projects WHERE id = $1";
        let params = List#{SqlValue.Int64(id)};
        RawSql.runWithAllow(() -> Sql.fetchOneAs(findContentVersionDecoder(), sql, params))

    ///
    /// bumpContentVersion: UPDATE projects SET content_version = content_version + 1 WHERE id = $1
    ///
    pub def bumpContentVersion(id: Int64): Int32 \ DbWrite =
        let sql = "UPDATE projects SET content_version = content_version + 1 WHERE id = $1";
        let params = List#{SqlValue.Int64(id)};
        RawSql.runWithAllow(() -> Sql.execute(sql, params))

}
